@page "/announcements"
@page "/announcements/{TenantIdParam:guid?}"
@using CoreBuilder.Models
@using CoreBuilder.Services
@using CoreBuilder.Data
@inject ContentService ContentService
@inject AppDbContext Context
@inject NavigationManager Navigation

<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3 class="fw-bold text-dark mb-0" style="font-size: 1.75rem;">📢 Duyuru Yönetimi</h3>
        @if (SelectedTenantId != Guid.Empty)
        {
            <button class="btn btn-primary shadow-sm" @onclick="OpenAddModal">
                <span class="oi oi-plus me-2"></span>Yeni Duyuru Ekle
            </button>
        }
    </div>

    @if (!string.IsNullOrEmpty(successMessage))
    {
        <div class="alert alert-success alert-dismissible fade show">
            <strong>✅</strong> @successMessage
            <button type="button" class="btn-close" @onclick="() => successMessage = string.Empty"></button>
        </div>
    }

    <div class="card shadow-sm border-0 mb-4">
        <div class="card-body p-4">
            <label class="form-label fw-bold mb-2">Site Seçin:</label>
            <select class="form-select" @bind="SelectedTenantId" @bind:event="onchange">
                <option value="@Guid.Empty">-- Seçiniz --</option>
                @foreach (var t in Tenants) { <option value="@t.Id">@t.Name</option> }
            </select>
        </div>
    </div>

    @if (SelectedTenantId != Guid.Empty && announcements.Any())
    {
        <div class="row g-3">
            @foreach (var announcement in announcements)
            {
                <div class="col-md-6 col-lg-4">
                    <div class="card shadow-sm h-100 @(announcement.IsImportant ? "border-danger" : "")">
                        @if (announcement.IsImportant)
                        {
                            <div class="card-header bg-danger text-white py-2">
                                <small><strong>⚠️ ÖNEMLİ DUYURU</strong></small>
                            </div>
                        }
                        @if (!string.IsNullOrEmpty(announcement.ImageUrl))
                        {
                            <img src="@announcement.ImageUrl" class="card-img-top" style="height: 150px; object-fit: cover;" />
                        }
                        <div class="card-body">
                            <h6 class="card-title fw-bold">@announcement.Title</h6>
                            <p class="card-text small text-muted">@announcement.Content.Substring(0, Math.Min(100, announcement.Content.Length))...</p>
                            <small class="text-muted">@announcement.PublishDate.ToString("dd MMM yyyy")</small>
                        </div>
                        <div class="card-footer bg-white border-0 d-flex gap-2">
                            <button class="btn btn-sm btn-outline-primary flex-grow-1" @onclick="() => OpenEditModal(announcement)">
                                <span class="oi oi-pencil me-1"></span>Düzenle
                            </button>
                            <button class="btn btn-sm btn-outline-danger" @onclick="() => DeleteAnnouncement(announcement.Id)">
                                <span class="oi oi-trash"></span>
                            </button>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else if (SelectedTenantId != Guid.Empty)
    {
        <div class="card shadow-sm border-0">
            <div class="card-body p-5 text-center text-muted">
                <span class="oi oi-megaphone" style="font-size: 2rem;"></span>
                <p>Henüz duyuru yok.</p>
            </div>
        </div>
    }
</div>

@if (showAddModal)
{
    <div class="modal d-block" style="background: rgba(0,0,0,0.5); z-index: 1050;">
        <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header bg-primary text-white">
                    <h5 class="modal-title">@(editingAnnouncementId == Guid.Empty ? "Yeni Duyuru" : "Duyuru Düzenle")</h5>
                    <button type="button" class="btn-close btn-close-white" @onclick="CloseModal"></button>
                </div>
                <div class="modal-body p-4">
                    <div class="mb-3">
                        <label class="form-label fw-bold">Başlık</label>
                        <input class="form-control" @bind="newTitle" />
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">İçerik</label>
                        <textarea class="form-control" @bind="newContent" rows="4"></textarea>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Resim URL (opsiyonel)</label>
                        <input class="form-control" @bind="newImageUrl" placeholder="https://..." />
                    </div>
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" @bind="newIsImportant" id="importantCheck">
                        <label class="form-check-label" for="importantCheck">Önemli Duyuru</label>
                    </div>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" @onclick="CloseModal">İptal</button>
                    <button class="btn btn-primary" @onclick="SaveAnnouncement">@(editingAnnouncementId == Guid.Empty ? "Yayınla" : "Güncelle")</button>
                </div>
            </div>
        </div>
    </div>
}

@code {
    [Parameter] public Guid? TenantIdParam { get; set; }
    
    private List<Tenant> Tenants = new();
    private List<Announcement> announcements = new();
    private Guid selectedTenantId;
    private bool showAddModal = false;
    private string successMessage = "";
    private Guid editingAnnouncementId = Guid.Empty;

    private string newTitle = "";
    private string newContent = "";
    private string newImageUrl = "";
    private bool newIsImportant = false;

    private Guid SelectedTenantId
    {
        get => selectedTenantId;
        set 
        { 
            selectedTenantId = value; 
            LoadAnnouncements();
            if (value != Guid.Empty)
            {
                Navigation.NavigateTo($"/announcements/{value}", false);
            }
        }
    }

    protected override void OnInitialized()
    {
        Tenants = Context.Tenants.ToList();
        
        if (TenantIdParam.HasValue && TenantIdParam.Value != Guid.Empty)
        {
            selectedTenantId = TenantIdParam.Value;
            LoadAnnouncements();
        }
    }

    protected override void OnParametersSet()
    {
        if (TenantIdParam.HasValue && TenantIdParam.Value != selectedTenantId)
        {
            selectedTenantId = TenantIdParam.Value;
            LoadAnnouncements();
        }
    }

    private async void LoadAnnouncements()
    {
        if (SelectedTenantId != Guid.Empty)
        {
            announcements = Context.Announcements
                .Where(a => a.TenantId == SelectedTenantId)
                .OrderByDescending(a => a.IsImportant)
                .ThenByDescending(a => a.PublishDate)
                .ToList();
            StateHasChanged();
        }
    }

    private void OpenAddModal()
    {
        editingAnnouncementId = Guid.Empty;
        newTitle = "";
        newContent = "";
        newImageUrl = "";
        newIsImportant = false;
        showAddModal = true;
    }

    private void OpenEditModal(Announcement announcement)
    {
        editingAnnouncementId = announcement.Id;
        newTitle = announcement.Title;
        newContent = announcement.Content;
        newImageUrl = announcement.ImageUrl ?? "";
        newIsImportant = announcement.IsImportant;
        showAddModal = true;
    }

    private void CloseModal()
    {
        showAddModal = false;
        editingAnnouncementId = Guid.Empty;
    }

    private async Task SaveAnnouncement()
    {
        if (string.IsNullOrWhiteSpace(newTitle) || string.IsNullOrWhiteSpace(newContent))
            return;

        if (editingAnnouncementId == Guid.Empty)
        {
            // Yeni duyuru
            var announcement = new Announcement
            {
                TenantId = SelectedTenantId,
                Title = newTitle,
                Content = newContent,
                ImageUrl = newImageUrl,
                IsImportant = newIsImportant
            };

            await ContentService.CreateAnnouncementAsync(announcement);
            successMessage = "Duyuru yayınlandı!";
        }
        else
        {
            // Duyuru güncelle
            var announcement = await Context.Announcements.FindAsync(editingAnnouncementId);
            if (announcement != null)
            {
                announcement.Title = newTitle;
                announcement.Content = newContent;
                announcement.ImageUrl = newImageUrl;
                announcement.IsImportant = newIsImportant;

                Context.Announcements.Update(announcement);
                await Context.SaveChangesAsync();
                successMessage = "Duyuru güncellendi!";
            }
        }

        showAddModal = false;
        editingAnnouncementId = Guid.Empty;
        LoadAnnouncements();
    }

    private async Task DeleteAnnouncement(Guid id)
    {
        await ContentService.DeleteAnnouncementAsync(id);
        successMessage = "Duyuru silindi!";
        LoadAnnouncements();
    }
}
